for i in $(cat notes/configparser_packages); do echo "================="; echo $i; echo "================="; cd $i; git checkout hacking; git fetch --all; git pull; git checkout -b configparser_import_fix; sed -i -E -e "s|from distutils import config|$(sed 's:/:\\/:g' ~/munka/devel/notes/configparser.txt)|" -e "s|config.ConfigParser\(\)|ConfigParser()|" setup.py docs/conf.py; git add setup.py docs/conf.py; git commit -m  "Fixing configparser imports for python 3.5.2"; git push bsipocz configparser_import_fix;cd ~/munka/devel;done
